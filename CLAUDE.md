# CLAUDE.md — Grafana Rightsizing Dashboard

## Overview
A Grafana dashboard (JSON model) for evaluating overcommitted Kubernetes clusters, providing rich visualizations of resource usage vs. requests/limits.

## Tech Stack
- Grafana dashboard JSON
- Prometheus (data source)
- Kubernetes metrics

## Development
Import `dashboard.json` into any Grafana instance connected to a Prometheus data source scraping Kubernetes metrics. No build step required.

## Architecture
- `dashboard.json` — the complete Grafana dashboard definition
- `images/` — screenshots of the dashboard views
- `docs/` — additional documentation

## Key Rules
- Dashboard ID on Grafana.com is 25087
- Requires Prometheus as data source with kube-state-metrics and cAdvisor
- Keep the JSON exportable and importable without manual edits
- No hardcoded cluster-specific values in the dashboard

*Generated by [LynxPrompt](https://lynxprompt.com) CLI*
